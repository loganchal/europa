name: Temporary dimension-9 data-format probe

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Format probe retained for reproducibility; automatic runs disabled.'
