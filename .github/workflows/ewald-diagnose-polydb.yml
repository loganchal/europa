name: Temporary Ewald polyDB diagnostic

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  diagnose:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Diagnostic retained for reproducibility; automatic runs disabled.'
