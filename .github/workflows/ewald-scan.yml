name: Temporary Ewald dimension-9 format probe

on:
  push:
    branches:
      - chatgpt-ewald-scan-20260811

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        facets: [10, 11, 18, 26]
    steps:
      - name: Inspect classification file
        shell: bash
        run: |
          set -euo pipefail
          url="https://polymake.org/polytopes/paffenholz/www/fv-09-${{ matrix.facets }}p.gz"
          echo "URL=$url"
          curl -fL --retry 3 "$url" -o data.gz
          echo "BYTES=$(wc -c < data.gz)"
          gzip -dc data.gz | head -n 24 || true
