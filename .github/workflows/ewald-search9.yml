name: Temporary exhaustive Ewald dimension-9 search

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Completed run 33685513693; automatic reruns disabled.'
